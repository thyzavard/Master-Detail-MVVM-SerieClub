<Window x:Class="Projet.Presentation.Forms.Views.test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet.Presentation.Forms.Views"
        xmlns:vm="clr-namespace:Projet.Presentation.Forms.ViewModel"
        mc:Ignorable="d"
        Title="test" Height="300" Width="300">
    <Window.DataContext>
        <vm:Test/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <Button Margin="20" Command="{Binding TestCommand}" Content="{Binding ProfilButtonText}"/>

        <Button Grid.Row="1" Margin="20" Command="{Binding ChangeTextCommand}" Content="Change le text"/>



    </Grid>
</Window>
